const arr =[{name:"Elie"},{name:'Tim'},{name:'Matt'},{name:'Colt'}]
function extractValue(arr, key){
acc.push(next[key]);
return acc;
}

function vowelCount(str){
    const vowels ="aeiou";
    return str. split('') .reduce (function9acc,next){
        let lowerCased = next.toLowerCase()
        if (vowels.indexOf(lower ))
    }
}
